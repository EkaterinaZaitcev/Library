http {
    server {
        listen 80;

        location /static/ {
            alias /app/static/;
            expires 30d;
        }
        location / {
            proxy_pass http://library;
        }
    }
}